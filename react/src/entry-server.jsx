import { StrictMode } from 'react'
// TODO: Import the renderToString function from React DOM server
// Hint: import { renderToString } from 'react-dom/server'

import App from './App'

/**
 * @param {string} _url
 */
export function render(_url) {
  // TODO: Render the React app to a string using React's server renderer
  // Hint: Use renderToString() with <StrictMode><App /></StrictMode>
  const html = null // Replace this line

  return { html }
}
